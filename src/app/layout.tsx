import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Navbar from '@/components/Navbar'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Rx Procures - Pharmaceutical Business Solutions',
  description: 'Complete B2B platform for pharmaceutical and healthcare companies providing ERP, CRM, POS software and marketplace for drugs, devices, and APIs.',
  keywords: 'pharmaceutical, healthcare, ERP, CRM, POS, marketplace, drugs, APIs, biologics, CDMO',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}